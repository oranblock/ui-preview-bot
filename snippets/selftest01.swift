struct Preview: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("UI Preview Bot")
            Button("It renders") {}
        }
        .padding(24)
    }
}
