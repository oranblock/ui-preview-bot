@Composable
fun Preview() {
    Column(modifier = Modifier.padding(24.dp)) {
        Text("UI Preview Bot")
        Spacer(Modifier.height(12.dp))
        Button(onClick = {}) { Text("It renders") }
    }
}
