package preview

import androidx.compose.runtime.Composable
import androidx.compose.material3.Text

// Placeholder. The render workflow overwrites this file with the snippet the
// user sent, which must define exactly one @Composable named Preview.
@Composable
fun Preview() {
    Text("send me a @Composable fun Preview()")
}
